<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_report_invoice" model="ir.actions.report">
        <field name="name">Hotel Invoice</field>
        <field name="model">reception.invoice</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">hms_app.report_invoice_document</field>
        <field name="report_file">hms_app.report_invoice_document</field>
        <field name="binding_model_id" ref="hms_app.model_reception_invoice"/>
        <field name="print_report_name">'Invoice - %s' % (object.invoice_number or 'Invoice').replace('/','')</field>
    </record>

</odoo>
