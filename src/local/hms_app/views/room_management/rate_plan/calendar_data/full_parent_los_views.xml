<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- Tree View -->
        <record id="view_full_parent_los_tree" model="ir.ui.view">
            <field name="name">rate_plan.full_parent_los.tree</field>
            <field name="model">rate_plan.full_parent_los</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="rate_plan_id" />
                    <field name="room_type_id" />
                    <field name="relevant_date" />
                    <field name="long_of_stay" />
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_full_parent_los_form" model="ir.ui.view">
            <field name="name">rate_plan.full_parent_los.form</field>
            <field name="model">rate_plan.full_parent_los</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="rate_plan_id" />
                            <field name="room_type_id" />
                            <field name="filter_id" />
                            <field name="relevant_date" />
                            <field name="long_of_stay" />
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>