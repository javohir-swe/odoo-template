<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- Tree View -->
        <record id="view_rate_plan_closed_tree" model="ir.ui.view">
            <field name="name">rate.plan.closed.tree</field>
            <field name="model">rate_plan.closed</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="rate_plan_id" />
                    <field name="room_type_id" />
                    <field name="relevant_date" />
                    <field name="status" />
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_rate_plan_closed_form" model="ir.ui.view">
            <field name="name">rate.plan.closed.form</field>
            <field name="model">rate_plan.closed</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="rate_plan_id" />
                            <field name="room_type_id" />
                            <field name="relevant_date" />
                            <field name="status" />
                            <field name="close_id" />
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>